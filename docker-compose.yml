version: "3"

services:
  app:
    container_name: nodejs-setup
    build:
      context: .
      dockerfile: Dokerfile
    volumes:
      - ./:/usr/src/app
    environment:
      - PORT=3000
    ports:
      - 3000:3000
